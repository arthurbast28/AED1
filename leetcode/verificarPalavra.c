#include <stdio.h>
#include <string.h>

int verificar_palavra(char palavra[]);

int main(){
    char palavra[30];

    printf("digite uma palavra: ");
    scanf("%s", palavra);
    
}

int verificar_palavra(char palavra[]){
    
    int tam = strlen(palavra), n = 1;
    char letra[n];


    if(palavra == NULL){
        return 0;
    }
    for(int i = 0; palavra[i] != '\0'; i++){

        if()
    }
    
}

